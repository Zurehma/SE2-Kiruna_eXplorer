import db from db;

function DocumentDAO() {
  this.getDocuments = () => {
    return new Promise((resolve, reject) => {});
  }

  this.addDocument = () => {
    return new Promise((resolve, reject) => {});
  }

  this.addLink = () => {
    return new Promise((resolve, reject) => {});
  }
}

export default DocumentDAO;
